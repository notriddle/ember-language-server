{
  "name": "ember-language-server",
  "description": "Language Server for Ember.js.",
  "version": "0.0.1",
  "author": "Ember Language Server Authors",
  "license": "MIT",
  "engines": {
    "node": "*"
  },
  "dependencies": {
    "vscode-languageserver": "^2.6.2"
  },
  "devDependencies": {
    "@types/node": "^6.0.52",
    "mocha": "^3.2.0",
    "mocha-tslint": "^0.2.0",
    "source-map-support": "^0.4.9",
    "tslint": "^4.3.1",
    "typescript": "^2.1.4"
  },
  "scripts": {
    "clean": "mkdir -p lib/ && rm -rf lib/ && mkdir -p lib/test",
    "compile": "tsc -p .",
    "watch": "tsc --watch -p .",
    "lint": "tslint --project ./tsconfig.json",
    "test": "npm run clean && npm run compile && mocha"
  }
}
